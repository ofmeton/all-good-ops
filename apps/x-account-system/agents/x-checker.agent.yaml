# GENERATED by `npm run ma:render` from TS SSOT (lib/ma/bootstrap-core.ts AGENT_MANIFESTS).
# 手で編集しない。プロンプト原文は SYSTEM_BUILDERS（compose/check/collector-prompts）。
name: x-checker
model:
  id: claude-haiku-4-5
system: ./x-checker.system.md
tools:
  - type: agent_toolset_20260401
    default_config:
      enabled: false
    configs:
      - name: web_search
        enabled: true
      - name: web_fetch
        enabled: true
  - type: custom
    name: submit_check
    description: 点検結果を提出する。重複・ファクトの判定と、人間が一目で分かる日本語の flag を渡すこと。これを呼んだら作業終了。
    input_schema:
      type: object
      properties:
        verdict:
          type: string
          enum:
            - ok
            - flag
          description: 総合判定。問題が1つでもあれば flag、無ければ ok。
        risk_level:
          type: string
          enum:
            - low
            - high
          description: 人間が注意して見るべき度合い。重複疑い・嘘疑いがあれば high。
        duplicate:
          type: string
          enum:
            - ok
            - similar
          description: 直近投稿との重複。ネタ（主題/具体内容）が被れば similar。
        factcheck:
          type: string
          enum:
            - ok
            - suspicious
            - unverifiable
          description: ファクト判定。完全な嘘/明らかに変な数字=suspicious、調べても不明=unverifiable、妥当=ok。
        source_grounded:
          type: boolean
          description: 主張・数字が元ネタツイートに含まれるか
        flags:
          type: array
          items:
            type: string
          description: 人間向けの日本語・簡潔な指摘。問題なければ空配列。
      required:
        - verdict
        - risk_level
        - duplicate
        - factcheck
        - source_grounded
      additionalProperties: false
