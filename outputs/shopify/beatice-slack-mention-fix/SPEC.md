# BEAT ICE Shopify → Slack 通知 メンション修正 仕様

作成: 2026-09-12 / 更新: 2026-09-12（Shopify実機確認ぶん反映） / 起点: 2026-09-11 みどさん定例MTG（[[raw/facts/situations/2026-09-11-beatice-midorikawa-mtg-minutes]]）
状態: **原因確定＋現行設定を実機確認済み。適用方式の選択待ち**

---

## 0. 実機で確認した現行設定（2026-09-12・Shopify管理画面）

ワークフローは1本だけ。Amazon用の別ワークフローは存在しない。

| 項目 | 実測値 |
|---|---|
| ワークフロー名 | `SpoonSlackにメンションお知らせ` |
| ID | `0198ef12-0120-75c6-a9bc-ac60442867fc` |
| トリガー | Order created |
| 条件 | Product product type is equal to `RICECREAM` |
| アクション | Send Slack message |
| Channel, or member id | `C055LCBLWNS` |
| メッセージ1行目 | `@田熊清太朗  @山口冴希` |
| 最終更新 | 2026-09-11 16:56 「裕美 翠川 による編集」＝みどさん |

現行メッセージ全文は `current-slack-message-2026-09-12.txt`（Flow の Message 欄からコピー）。
証跡: `evidence-flow-send-slack-message-2026-09-12.jpg` / `evidence-flow-version-history-2026-09-12.jpg`

### 0-1. みどさんの MTG 後の編集で状況が悪化している

2026-08-30 の実際の通知（さとしさん撮影・`evidence-slack-mention-render-2026-08-30.png`）では、
メンション行は `@佐々木 @mikikotamaki23 @山口冴希` だった。画像上で

- `@mikikotamaki23` → **青いリンク（=メンション成立）**
- `@佐々木` `@山口冴希` → **黒い平文（=メンション不成立）**

つまり ASCII ハンドルの1人だけが届いていた。これが §1 の原因仮説の直接証拠。

その後 2026-09-11 16:56 にみどさんが `@田熊清太朗  @山口冴希` へ差し替えた結果、
**唯一届いていた `@mikikotamaki23` が消え、現在は誰にもメンションが飛んでいない**。
「表記を英語にする」方針のままだと、この種の踏み外しが毎回起きる。

### 0-2. 人物の対応関係（vault 接地済み）

- 「琢磨氏」（議事録の表記）＝ **田熊清太朗さん**（BEAT ICE 現地マネージャー、契約は3月末まで）
- 「さきさん」＝ **山口冴希さん**
- 「みどさん」＝ 緋呂美・ミドリカワ氏（Shopify アカウント表示名は `裕美 翠川`）
- 「パマ君」＝ 新マネージャー。本名・Slackアカウント未確定（みどさんからの引き継ぎ待ち）
- 「田崎氏ら」（議事録表記）＝ 実際の旧メンション先は `佐々木` と `mikikotamaki23`

---

## 1. 本質的な問題（課題文の再定義）

MTGでの課題文は「漢字アカウント名だとメンションが飛ばないので、アカウント名を半角英語に変える」だった。
調査の結果、**これは症状への対処であって原因の修正ではない**。

原因は「名前が漢字だから」ではなく、**通知メッセージが `@名前` という平文でメンションを書いている**こと。

- Slack の bot/webhook 投稿で `@名前` を平文で書くと、Slack は **display name（表示名）ではなく username（＝ASCII の @ハンドル）** に対してマッチングを試みる。この `link_names` によるマッチングは **2017-09 に非推奨化済み**。
- `display_name` は UTF-8 フルレンジ（漢字可）だが、`name`（ハンドル）は別物。よって漢字表示名の人は平文 `@漢字` では**構造的に永久に一致しない**。半角英語名の人だけ「たまたま」一致していた。
- 唯一サポートされた形式は **`<@U012AB3CD>`（メンバーID）**。

出典（一次情報・確認日 2026-09-12）:
- https://docs.slack.dev/messaging/formatting-message-text — 「To mention a user, provide their user ID with the following syntax: `Hey <@U012AB3CD>, ...`」
- https://docs.slack.dev/changelog/2017-09-the-one-about-usernames/ — 「Using `link_names` when posting messages is also deprecated ... please mention users with the `<@W123>` user ID format instead」「`display_name` is not unique and may contain a relatively full gamut of UTF-8 characters」
- https://help.shopify.com/en/manual/shopify-flow/reference/connectors/send-slack-message — 「You can format your message using Slack's mrkdwn syntax」= Flow の Message 欄は mrkdwn をそのまま通すので `<@U...>` が使える

## 2. 修正方式の選択肢（3案）

Slack が公式にサポートする書式は3つだけ（出典・確認日 2026-09-12: https://docs.slack.dev/messaging/formatting-message-text）。

| | 案A: `<!channel>` | 案B: メンバーID `<@U…>` | 案C: ユーザーグループ `<!subteam^S…>` |
|---|---|---|---|
| 必要な事前情報 | **なし** | 全員のメンバーID | グループID 1個（要グループ作成） |
| 今すぐ適用できるか | **できる** | さとしさん待ち | Slack側で作成が必要 |
| 通知先 | `#受注通知` の全メンバー | 指定した人だけ | グループのメンバー |
| 人事異動時の作業 | **なし**（チャンネル出入りだけ） | Flow を編集し直す | Slack のグループ編集だけ |
| 表示名変更で再発 | しない | しない | しない |
| 想定リスク | 対象外の人も鳴る | ID 抜け漏れ・交代で放置 | 初期セットアップ1回 |

### 決定（2026-09-12・本人）
**案B（メンバーID `<@U…>`）を採用**。適用作業も私（エージェント）が行う。
→ さとしさんから3名分のメンバーIDが届き次第、Flow の1行目を差し替えて実機検証する。

以下は選定時の比較材料として残す。

**私の推奨は案A**だった。理由: ①IDが1つも要らず今すぐ直る ②田熊さんが3月に抜けパマ君が入る＝通知先が今後も動くのに、Flow を触るのは陸さんしかできない ③通知先の正本が「`#受注通知` に誰が居るか」になり、Slack 内で完結する。
`#受注通知` が受注通知専用チャンネルである限り、鳴ってほしくない人はそもそも居ない。

案B が優位なのは「同じチャンネルに、通知されたくないメンバーが常駐している」場合のみ。

いずれの案でも **MTGで決めた「アカウント名を半角英語に変える」は不要**。
琢磨氏（田熊さん）への改名依頼も、みどさん側の「メンション設定を英語表記に変更」も止めてよい。

## 3. 適用手順

### 3-1. 差し替える1行目

差し替えるのは **メッセージ本文の1行目だけ**。2行目以降（Liquid の日時計算・注文明細）は一切触らない。

案A:
```
<!channel>
```

案B（IDは実値に置換）:
```
<@U_PAMA____> <@U_SAKI____>
```

案C:
```
<!subteam^S________>
```

適用後の全文は `fixed-slack-message-plan-a.txt` / `fixed-slack-message-plan-b.txt` に用意済み。

### 3-2. メンバーIDの採取（案B・案C を選んだ場合のみ）
**さとしさんに聞かなくても自力で取れる**（そのワークスペースのメンバーなら誰でも可）:
1. Slack でその人のプロフィールを開く
2. 「その他（…）」→ **「メンバーIDをコピー」**
3. `U` で始まる文字列（例 `U012AB3CD`）を控える

陸さんが BEAT ICE のワークスペースに入っていない場合だけ、さとしさんに依頼する。依頼文はこれで足りる:

> 受注通知の Slack メンションを直すので、下記のメンバーIDを教えてください。
> Slack でその人のプロフィール → 「…」→「メンバーIDをコピー」で出る `U` から始まる文字列です。
> ・パマ君（新マネージャー）
> ・山口冴希さん
> ・田熊清太朗さん（3月まで残すなら）

### 3-3. Shopify Flow を修正
1. Shopify 管理画面 → アプリ → **Flow** → `SpoonSlackにメンションお知らせ`
2. 「編集」→ **Send Slack message** をクリック
3. Message 欄の1行目を §3-1 の書式に差し替え
4. 保存 → 有効化（既に アクティブ なので保存のみ）
5. Channel, or member id 欄（`C055LCBLWNS`）は触らない

### 3-3b. Flow エディタの実機挙動（2026-09-12 確認）
- エディタ URL: `https://admin.shopify.com/store/beatice/apps/flow/editor/0198ef12-0120-75c6-a9bc-ac60442867fc/01M27QGEQ1V7EVACSEXA7JAQGH`
- **オートセーブ**。画面右下に「保存済み ◯月◯日 ◯:◯◯ に最終変更」と出るだけで、明示的な保存ボタンは無い。→ 編集すると即本番に反映される。下書き状態で放置できないので、貼る文字列を確定してから触る。
- Message 欄は行番号付きのコードエディタ。1行目だけを選択して差し替えられる。
- Channel 欄のヒント表記は `$member-id or #channel. To post in a channel add "Flowbot" app in Slack`。現在は接頭辞なしの生チャンネルID `C055LCBLWNS` が入っていて、実際に配信されているのでそのまま触らない。
- キャンバス上部に「ワークフローをテストする」があり、既存注文（例 `注文 #1072`）を選んで実行できる。**テスト実行は本物の Slack 投稿を出す**ので、メンション先の人が実際に鳴る。実行前に一声かける。

### 3-4. 必須の事前確認
- `C055LCBLWNS`（`#受注通知`）に **FlowBot が参加しているか**。8/30 に FlowBot の投稿が届いているので参加済みと判断できる。
- 案A を使う場合、FlowBot に `<!channel>` を鳴らす権限があるか。投稿が通っているので通常は問題ないが、テスト注文で実機確認する。

### 3-4. 検証（完了条件）
「テスト緑」ではなく**実機で通知が届くこと**を完了条件とする。
1. Flow のテスト実行、または少額のテスト注文を1件作成
2. Slack で通知を確認。合格条件:
   - メンション部分が **青いリンク**として表示されている（平文の `@名前` のままなら失敗）
   - 対象者の端末に**通知バッジ／プッシュが実際に届いた**ことを本人に口頭確認
3. 琢磨氏（漢字表示名のまま）で届けば、原因仮説が実証されたことになる

## 4. ブロッカーの現況（2026-09-12 時点）

1. ~~**Shopify 管理画面へのログインセッション**~~ → **解消**。2026-09-12 に陸さんが Mac mini のブラウザでログイン済み。Flow の読み書きが可能になった（Flow は Admin API 非対応なので管理画面UI経由のみ、という制約は変わらない）。
2. **BEAT ICE の Slack ワークスペースへのアクセス** — 未解消。このマシンの Slack bot はワークスペース `riku`（Milla用、チャンネル `C0BTT9QLT53` / `C0BULALNYKE`）のみ。`C055LCBLWNS` には到達できないため、メンバーIDを自動採取できない。
   → **案A を選べばこのブロッカーは不要になる。**
3. **「パマ君」の特定** — 未解消。本名・Slackアカウントが未確定（みどさんからの引き継ぎ待ち）。
   → **案A を選べばこれも不要になる**（パマ君が `#受注通知` に入るだけで通知が届く）。

→ 残る決定事項は「案A / 案B / 案C のどれにするか」だけ。案Aなら5分で適用・検証まで完了できる。

## 5. 関連タスク（MTG由来・本件と分離）
- [~~工藤陸~~] 琢磨氏アカウント名の英語化依頼 → **本方式では不要**（依頼を止める）
- [みどさん] Slackメンション設定を英語表記に変更 → **本方式では不要**
- [全体] 9月中にシステムアップデート完了・運用開始
